services:

  mongo:
    image: mongo:6
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    env_file:
      - .env

  auth:
    build:
      context: .
      dockerfile: auth_service/Dockerfile
    working_dir: /app
    volumes:
      - ./auth_service:/app/auth_service
    ports:
      - "8001:8001"
    command: uvicorn auth_service.main:app --host 0.0.0.0 --port 8001
    depends_on:
      - mongo
    env_file:
      - .env

  stock:
    build:
      context: .
      dockerfile: stock_service/Dockerfile
    working_dir: /app
    volumes:
      - ./stock_service:/app/stock_service
    ports:
      - "8002:8002"
    command: uvicorn stock_service.main:app --host 0.0.0.0 --port 8002
    env_file:
      - .env

  model:
    build:                       # ← use your model Dockerfile
      context: .
      dockerfile: app/Dockerfile
    working_dir: /app            # /app is where main.py lives
    volumes:
      - ./app:/app               # hot‑reload code without rebuilds
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    env_file:
      - .env

  tests:
    build:
      context: .
      dockerfile: tests/Dockerfile
    working_dir: /code
    depends_on:
      - auth
      - stock
      - model
    # run once, then exit; no need for ports or volumes
    restart: "no"
    env_file:
      - .env

volumes:
  mongo_data:

